# Prism / Saathi — environment template (safe to commit)
# Copy this file to .env.local and paste your real key there.
#
#   cp .env.example .env.local
#
# Variable name MUST stay GEMINI_API_KEY — the app reads exactly this name.

GEMINI_API_KEY=your_key_here
